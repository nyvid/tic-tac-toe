import { GameBoard } from 'components/GameBoard/GameBoard';

const Game = () => {
  return (
    <div className="max-w-2xl m-auto w-full h-full">
      <GameBoard />
    </div>
  );
};

export default Game;
